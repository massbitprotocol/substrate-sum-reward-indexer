{
  "name": "sum-reward",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/massbit codegen"
  },
  "homepage": "",
  "repository": "",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "devDependencies": {
    "@massbit/cli": "0.2.0",
    "@massbit/types": "0.1.0",
    "@polkadot/api": "^6",
    "typescript": "^4.1.3"
  }
}